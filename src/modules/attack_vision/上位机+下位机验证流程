下面给你一套**结合当前 PX4 `attack_vision` 状态机 + `/home/www/uav_ws` 上位机工程**的仿真验证步骤。

我先说结论：
你忘记的 `external_mission_active = false` 命令，在 ROS2 侧可以直接这样发：

```bash
ros2 topic pub --once /uav/px4/external_mission_active std_msgs/msg/Bool "{data: false}"
```

如果要持续发布 false，建议用：

```bash
ros2 topic pub -r 10 /uav/px4/external_mission_active std_msgs/msg/Bool "{data: false}"
```

不过注意：如果你启动了 `/home/www/uav_ws` 的 `offboard_coordinator_node`，它本身会周期发布 `/uav/px4/external_mission_active`，所以**手动发布可能会被 coordinator 覆盖**。更推荐通过 `gimbal_status` 的仿真锁定参数控制它。

---

# 1. 当前上位机代码里的控制权逻辑

我看了 `/home/www/uav_ws/src/uav_mission_control/src/offboard_coordinator_node.cpp`，当前核心逻辑是：

```text
RC/飞控 Offboard 有效 + 吊舱未锁定
    => 上位机任务 active
    => mission_enable = true
    => external_mission_active = true

RC/飞控 Offboard 有效 + 吊舱已锁定
    => 上位机让权给 PX4 attack_vision
    => mission_enable = false
    => external_mission_active = false
```

也就是说，在仿真里你不一定需要手动发 `external_mission_active=false`。
只要让 `gimbal_status_node` 的 `sim_lock_override=true`，`offboard_coordinator_node` 就会自动发布：

```text
/uav/px4/external_mission_active = false
```

---

# 2. 相关 ROS2 话题和参数

## 2.1 上位机发布给 PX4 的话题

`offboard_coordinator_node` 发布：

```text
/uav/px4/external_mission_active
```

类型是：

```text
std_msgs/msg/Bool
```

含义：

| 值 | 含义 |
|---|---|
| `true` | 上位机正在控制，PX4 `attack_vision` 必须让路 |
| `false` | 上位机让权，PX4 `attack_vision` 可以在条件满足时接管 |

---

## 2.2 仿真吊舱锁定参数

`gimbal_status_node` 有参数：

```text
/gimbal_status sim_lock_override
```

在仿真模式下：

| 参数值 | 效果 |
|---|---|
| `false` | 吊舱未锁定 |
| `true` | 吊舱锁定 |

对应命令：

```bash
ros2 param set /gimbal_status sim_lock_override true
```

取消锁定：

```bash
ros2 param set /gimbal_status sim_lock_override false
```

---

## 2.3 上位机内部吊舱状态话题

`gimbal_status_node` 发布：

```text
/uav/gimbal/status
```

`offboard_coordinator_node` 订阅它，并判断：

```cpp
gimbal_locked_ = msg->lock_active && msg->frame_valid;
```

所以仿真中：

```bash
ros2 param set /gimbal_status sim_lock_override true
```

之后，coordinator 会认为：

```text
gimbal_locked = true
```

然后发布：

```text
external_mission_active = false
```

---

# 3. 推荐仿真验证目标

这次你主要验证的是最新重构后的 `Run()` 状态机，所以建议分 5 个场景：

1. **未锁定时，上位机 active，PX4 attack_vision 让路**
2. **锁定后，上位机让权，PX4 attack_vision 进入 SWITCHING_TO_OFFBOARD**
3. **飞控进入 Offboard 后，PX4 attack_vision 进入 OFFBOARD 并执行 `handle_guidance()`**
4. **取消锁定后，PX4 attack_vision 退出制导并悬停**
5. **重新上位机 active 时，attack_vision 不主动切 HOLD，只让路**

---

# 4. 终端规划

建议开 5 个终端：

| 终端 | 用途 |
|---|---|
| 终端 A | PX4 SITL |
| 终端 B | ROS2 上位机 launch |
| 终端 C | PX4 shell / attack_vision 启动与日志 |
| 终端 D | ROS2 topic/param 观测 |
| 终端 E | 手动发布/切换测试命令 |

---

# 5. 第一步：启动 PX4 SITL

在 PX4 工程目录：

```bash
cd /home/www/px4_wlh/px4rt
make px4_sitl gz_x500
```

如果你的工程用的是其他模型，比如之前用的是 `iris` 或自定义模型，就换成你实际能跑通的目标。

启动后进入 PX4 shell。

---

# 6. 第二步：启动上位机 ROS2 仿真节点

新终端：

```bash
cd /home/www/uav_ws
source install/setup.bash
ros2 launch uav_mission_control sim.launch.py
```

这个 launch 会启动：

- `mavros`
- `offboard_coordinator_node`
- `mission_manager_node`
- `gimbal_status_node`
- `swarm_link_node`
- `ground_station_mission_server_node`
- `collision_avoidance_node`

其中和本次验证最相关的是：

```text
/offboard_coordinator
/gimbal_status
```

---

# 7. 第三步：确认上位机话题存在

新终端：

```bash
cd /home/www/uav_ws
source install/setup.bash
ros2 topic list | grep -E "external_mission|gimbal|coordinator|mission_enable"
```

你应该看到类似：

```text
/uav/px4/external_mission_active
/uav/gimbal/status
/offboard_coordinator/coordinator_state
/offboard_coordinator/mission_enable
```

---

# 8. 第四步：观测 external_mission_active

查看当前上位机是否 active：

```bash
ros2 topic echo /uav/px4/external_mission_active
```

刚启动、吊舱未锁定时，如果飞控 Offboard 条件还没满足，可能是：

```text
data: false
```

当飞控进入 Offboard 且吊舱未锁定，上位机开始发任务 setpoint 后，应该变成：

```text
data: true
```

这时 PX4 `attack_vision` 应该让路。

---

# 9. 第五步：让飞控解锁并进入 Offboard

在 PX4 shell 中执行：

```bash
commander arm
commander mode offboard
```

如果你的 PX4 版本要求先有 setpoint 才能进 Offboard，那么上位机 `mission_manager` 会通过 `setpoint_warmup_sec` 预热。也可以先观察：

```bash
ros2 topic echo /offboard_coordinator/coordinator_state
```

重点看：

```text
rc_offboard
gimbal_locked
mode
publishing_setpoints
detail
```

在仿真中，上位机代码允许：

```text
sim_accept_fc_offboard = true
```

所以即使没有真实 RC，只要：

```text
PX4 mode = OFFBOARD
PX4 armed = true
```

coordinator 也认为 Offboard 有效。

---

# 10. 第六步：启动 PX4 attack_vision 模块

在 PX4 shell 里执行：

```bash
attack_vision start
```

如果模块名不是 `attack_vision`，你可以先查：

```bash
help
```

或者在 PX4 shell 里试：

```bash
attack_vision status
```

启动成功后，日志应该出现类似：

```text
=== Attack Vision Run() Started ===
Attack Vision module starting...
Virtual UART port opened successfully
Simulation Mode: waiting for commander mode OFFBOARD before guidance
【仿真模式】固定吊舱NED目标姿态初始化
```

如果参数没开，先设置：

```bash
param set AAATTKVIS_EN 1
```

然后再：

```bash
attack_vision start
```

---

# 11. 第七步：验证“上位机 active 时 attack_vision 让路”

保持吊舱未锁定：

```bash
ros2 param set /gimbal_status sim_lock_override false
```

观察 coordinator：

```bash
ros2 topic echo /offboard_coordinator/coordinator_state
```

期望：

```text
gimbal_locked: false
publishing_setpoints: true
detail: Offboard + 未锁定，机载执行航路规划
```

观察 external：

```bash
ros2 topic echo /uav/px4/external_mission_active
```

期望：

```text
data: true
```

此时 `attack_vision` 最新状态机应该走最高优先级让路分支：

```text
External mission active -> attack_vision idle
module_state = HOLD
不切 HOLD
不执行 handle_guidance()
```

这是第一项关键验证。

---

# 12. 第八步：让吊舱锁定，触发上位机让权

执行：

```bash
ros2 param set /gimbal_status sim_lock_override true
```

查看吊舱状态：

```bash
ros2 topic echo /uav/gimbal/status
```

期望：

```text
frame_valid: true
lock_active: true
lock_state: 1
servo_state: 7
```

查看 coordinator：

```bash
ros2 topic echo /offboard_coordinator/coordinator_state
```

期望：

```text
gimbal_locked: true
mode: MODE_ATTACK_YIELD
publishing_setpoints: false
detail: 吊舱已锁定，末制导由飞控 attack_vision 执行，机载停止发布设定点
```

查看 external：

```bash
ros2 topic echo /uav/px4/external_mission_active
```

期望：

```text
data: false
```

这就是你忘记的 `external_mission_active=false` 的自动触发方式。

---

# 13. 如果你要手动发 external_mission_active=false

如果你没有启动 coordinator，或者只是想单独验证 PX4 状态机，可以手动发布：

```bash
ros2 topic pub --once /uav/px4/external_mission_active std_msgs/msg/Bool "{data: false}"
```

更稳的是持续发布：

```bash
ros2 topic pub -r 10 /uav/px4/external_mission_active std_msgs/msg/Bool "{data: false}"
```

如果要模拟上位机 active：

```bash
ros2 topic pub -r 10 /uav/px4/external_mission_active std_msgs/msg/Bool "{data: true}"
```

注意：
如果 `offboard_coordinator_node` 也在运行，它会继续发布同一个话题，手动发布可能和它打架。
所以二选一：

- 要验证完整系统：用 `ros2 param set /gimbal_status sim_lock_override true/false`
- 要单独验证 PX4：不要启动 coordinator，手动发布 `/uav/px4/external_mission_active`

---

# 14. 第九步：观察 PX4 attack_vision 状态机进入末制导

当满足：

```text
仿真 RC = MODE_OFFBOARD
external_mission_active = false
lock_active = true
frame_valid_recent = true
vehicle_armed = true
```

则：

```text
can_control = true
```

状态机应该从：

```text
HOLD
```

进入：

```text
SWITCHING_TO_OFFBOARD
```

如果飞控已经是 Offboard，则直接进入：

```text
OFFBOARD
```

你在 PX4 日志中应该看到类似：

```text
末制导条件满足，开始切换到Offboard模式
```

或：

```text
已在Offboard模式 - 开始末制导(仿真模式)
```

随后应该看到：

```text
Offboard切换完成 - 开始末制导
```

之后每 50ms 调用一次：

```text
handle_guidance()
```

如果 `PX4_DEBUG` 没显示，可以关注 `trajectory_setpoint` 或状态日志。

---

# 15. 第十步：观察 PX4 侧 uORB 状态

在 PX4 shell 中可以观察：

```bash
listener vehicle_status
```

重点看：

```text
nav_state
arming_state
```

期望：

```text
arming_state = ARMED
nav_state = OFFBOARD
```

查看 `attack_vision_status`：

```bash
listener attack_vision_status
```

期望锁定后：

```text
lock_active: true
rc_offboard: true
external_mission_active: false
allow_takeover: true
```

注意：
`allow_takeover=true` 还不等于 `can_control=true`，因为 `can_control` 还要求：

```text
frame_valid_recent = true
vehicle_armed = true
```

但你的日志里已经加了：

```text
frame_valid
can_control
module_state
```

所以也可以直接看 PX4 控制台 5 秒周期日志。

---

# 16. 第十一步：观察上位机让权后是否停止发 setpoint

当锁定后：

```bash
ros2 param set /gimbal_status sim_lock_override true
```

查看：

```bash
ros2 topic echo /offboard_coordinator/mission_enable
```

期望：

```text
data: false
```

这表示上位机停止任务 setpoint 发布。

同时：

```bash
ros2 topic echo /uav/px4/external_mission_active
```

期望：

```text
data: false
```

这表示 PX4 `attack_vision` 可以接管。

---

# 17. 第十二步：验证失锁退出

执行：

```bash
ros2 param set /gimbal_status sim_lock_override false
```

这会导致：

```text
/uav/gimbal/status lock_active=false
```

coordinator 切回任务模式：

```text
external_mission_active=true
```

PX4 `attack_vision` 侧按当前状态机最高优先级会：

```text
External mission active -> attack_vision idle
module_state = HOLD
不主动 switch_to_hold
```

注意这里有一个重要现象：

由于你当前设计是：

```text
上位机 active 时 attack_vision 让路，不主动切 HOLD
```

所以如果失锁同时 coordinator 恢复 active，那么 `attack_vision` 不会主动切 Loiter，而是让上位机继续任务控制。

如果你想单独验证“失锁 -> attack_vision 切 HOLD”，需要在不启动 coordinator 或不让它把 `external_mission_active` 改成 true 的情况下，让 PX4 侧吊舱失锁。否则最高优先级会被 external active 抢先处理。

---

# 18. 单独验证 PX4 失锁悬停的方法

如果你要验证这条状态机分支：

```text
OFFBOARD 中 can_control=false -> switch_to_hold()
```

建议这样做：

1. 不启动 `offboard_coordinator_node`，或者停止它；
2. 手动持续发布：

```bash
ros2 topic pub -r 10 /uav/px4/external_mission_active std_msgs/msg/Bool "{data: false}"
```

3. 让 PX4 attack_vision 进入 Offboard 末制导；
4. 停止吊舱帧或让 PX4 侧 `_lock_active=false`；
5. 观察 PX4 日志：

```text
末制导条件丢失 -> 悬停
已发送切换到悬停模式命令
```

但是当前 PX4 仿真吊舱帧来自 `VSerial`，不是 ROS 的 `/uav/gimbal/status`。所以完整地触发 PX4 侧 `_lock_active=false`，要看 `VSerial` 的仿真帧发送逻辑是否随 ROS `gimbal_status` 变化。如果它们没打通，则 ROS 的 `sim_lock_override=false` 只影响上位机 coordinator，不一定直接影响 PX4 内部 `_lock_active`。

---

# 19. 验证 `external_mission_active=false` 的最短命令

如果你只是忘了命令，这里直接给你：

## 发布一次 false

```bash
cd /home/www/uav_ws
source install/setup.bash
ros2 topic pub --once /uav/px4/external_mission_active std_msgs/msg/Bool "{data: false}"
```

## 持续发布 false

```bash
cd /home/www/uav_ws
source install/setup.bash
ros2 topic pub -r 10 /uav/px4/external_mission_active std_msgs/msg/Bool "{data: false}"
```

## 发布 true，模拟上位机 active

```bash
cd /home/www/uav_ws
source install/setup.bash
ros2 topic pub -r 10 /uav/px4/external_mission_active std_msgs/msg/Bool "{data: true}"
```

---

# 20. 推荐完整验证顺序

## 终端 A：PX4 SITL

```bash
cd /home/www/px4_wlh/px4rt
make px4_sitl gz_x500
```

PX4 shell：

```bash
param set AAATTKVIS_EN 1
commander arm
commander mode offboard
attack_vision start
```

---

## 终端 B：上位机 launch

```bash
cd /home/www/uav_ws
source install/setup.bash
ros2 launch uav_mission_control sim.launch.py
```

---

## 终端 C：观察 coordinator

```bash
cd /home/www/uav_ws
source install/setup.bash
ros2 topic echo /offboard_coordinator/coordinator_state
```

---

## 终端 D：观察 external_mission_active

```bash
cd /home/www/uav_ws
source install/setup.bash
ros2 topic echo /uav/px4/external_mission_active
```

---

## 终端 E：控制吊舱锁定

未锁定：

```bash
cd /home/www/uav_ws
source install/setup.bash
ros2 param set /gimbal_status sim_lock_override false
```

锁定：

```bash
ros2 param set /gimbal_status sim_lock_override true
```

---

# 21. 关键期望现象

## 未锁定阶段

```text
gimbal_locked = false
mission_enable = true
external_mission_active = true
attack_vision module_state = HOLD
attack_vision 不发末制导
```

## 锁定阶段

```text
gimbal_locked = true
mission_enable = false
external_mission_active = false
attack_vision allow_takeover = true
attack_vision can_control = true
attack_vision module_state = SWITCHING_TO_OFFBOARD 或 OFFBOARD
attack_vision 开始 handle_guidance()
```

## 重新失锁阶段

```text
gimbal_locked = false
external_mission_active = true
attack_vision 让路
上位机恢复任务控制
```

---

# 22. 如果 PX4 attack_vision 一直不接管，按这个顺序排查

## 22.1 看 external 是否为 false

```bash
ros2 topic echo /uav/px4/external_mission_active
```

必须是：

```text
data: false
```

---

## 22.2 看 PX4 是否收到 external false

PX4 shell：

```bash
listener attack_vision_status
```

确认：

```text
external_mission_active: false
```

如果 ROS 是 false，但 PX4 里还是 true，说明 ROS 到 uORB 的桥接没打通。

---

## 22.3 看 PX4 侧 lock 是否 true

PX4 shell：

```bash
listener attack_vision_status
```

确认：

```text
lock_active: true
```

如果 ROS `/uav/gimbal/status` 是 true，但 PX4 `attack_vision_status.lock_active` 是 false，说明：

```text
ROS gimbal_status_node 只影响上位机 coordinator，
没有直接影响 PX4 attack_vision 的 VSerial 吊舱帧
```

这时需要启动 `/home/www/uav_ws/tools/sim_gimbal_sender.py` 或检查 `VSerial` 仿真帧来源。

---

## 22.4 看飞控是否解锁

PX4 shell：

```bash
listener vehicle_status
```

需要：

```text
arming_state = ARMED
```

---

## 22.5 看飞控是否 Offboard

```bash
listener vehicle_status
```

需要：

```text
nav_state = OFFBOARD
```

如果还不是 Offboard，`attack_vision` 会在 `SWITCHING_TO_OFFBOARD` 状态里尝试切换。

---

# 23. 关于 `sim_gimbal_sender.py`

工程里有：

```text
/home/www/uav_ws/tools/sim_gimbal_sender.py
```

这个脚本大概率用于向仿真吊舱/虚拟串口发送模拟吊舱帧。
如果你发现：

```text
ROS /uav/gimbal/status lock_active=true
```

但 PX4：

```text
listener attack_vision_status
```

里：

```text
lock_active=false
```

那说明 PX4 `attack_vision` 没收到自己的虚拟吊舱帧，需要运行或检查这个脚本。

可以先查看它的帮助：

```bash
cd /home/www/uav_ws
python3 tools/sim_gimbal_sender.py --help
```

如果没有帮助信息，可以直接运行：

```bash
python3 tools/sim_gimbal_sender.py
```

然后再看 PX4 日志是否出现：

```text
成功解析帧: lock=1
```

---

# 24. 最推荐的一条验证路径

如果你要验证“完整上位机让权 + PX4 接管”，推荐按这个顺序：

```bash
# 1. 启动 PX4 SITL
cd /home/www/px4_wlh/px4rt
make px4_sitl gz_x500

# 2. 另一个终端启动 ROS2 上位机
cd /home/www/uav_ws
source install/setup.bash
ros2 launch uav_mission_control sim.launch.py

# 3. PX4 shell
param set AAATTKVIS_EN 1
commander arm
commander mode offboard
attack_vision start

# 4. ROS2 先设置未锁定
ros2 param set /gimbal_status sim_lock_override false

# 5. 观察 external 应该是 true，attack_vision 让路
ros2 topic echo /uav/px4/external_mission_active

# 6. 设置锁定
ros2 param set /gimbal_status sim_lock_override true

# 7. external 应该变 false
ros2 topic echo /uav/px4/external_mission_active

# 8. PX4 shell 看 attack_vision 状态
listener attack_vision_status
listener vehicle_status
```

如果 PX4 侧 `lock_active` 还是 false，再补跑：

```bash
cd /home/www/uav_ws
python3 tools/sim_gimbal_sender.py
```

---

# 25. 最后总结

你要的 `external_mission_active=false` 命令是：

```bash
ros2 topic pub --once /uav/px4/external_mission_active std_msgs/msg/Bool "{data: false}"
```

但在当前 `/home/www/uav_ws` 上位机工程里，更推荐通过：

```bash
ros2 param set /gimbal_status sim_lock_override true
```

让 `offboard_coordinator_node` 自动发布：

```text
/uav/px4/external_mission_active = false
```

因为这更符合真实逻辑：

```text
吊舱锁定
=> 上位机停止发布任务 setpoint
=> external_mission_active=false
=> PX4 attack_vision 接管
```
