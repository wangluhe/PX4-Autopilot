# AGENTS.md

## 项目背景

这是一个px4飞控末制导项目，主要进行无人机末制导，项目代码在src/modules/attack_vision.
相关机载电脑的程序在/home/www/uav_ws工程

## 技术栈



## 工作规则

### 开始前

- 修改代码前，先阅吊舱的通信协议CGTD055串口通讯协议.pdf和CGTD055微型三轴双光稳像吊舱技术规格书V2.1.pdf

### 变更边界

- 默认只改PX4 源码，不改机载电脑侧。
- 路径尽量使用 `$HOME/uav_ws`、`$HOME/px4_wlh/px4rt` 这类通用写法，避免写死 `/home/www`。
- 不要随意删除文件、清空结果或改动用户已有内容。

### 数据与配置

- 涉及删除文件、删除结果、清空数据库时，必须先说明风险，并给出二次确认、操作日志和回滚说明。
- 涉及参数默认值、话题命名、端口号、TTL、超时动作时，要同步更新代码和文档。

### 修改后必须提供

- 受影响文件列表。
- 每个文件改了什么。
- 启动命令。
- 测试步骤。
- 关键话题、节点或日志点。
- 风险、限制和回滚方式。

## 输出规范

### 回答方式

- 先说结论，再给必要细节。
- 如果只是小改动，保持简短。
- 如果是结构性改动，说明调用关系、验证路径和未覆盖风险。
- 不要把所有内容塞进一个大段里，优先短段落。

## 完成标准

每次任务完成后必须说明：

1. 修改了哪些文件。
2. 每个文件修改了什么。
3. 如何启动后端。
4. 如何启动前端。
5. 如何验证功能。
6. 是否涉及数据库 SQL。
7. 是否存在风险和回滚方式。
